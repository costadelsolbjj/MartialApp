@model IEnumerable<MartialApp.Models.EventTrainer>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Trainers)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Event)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Event.Start)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Trainers.UserName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Event.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Event.Start)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.EventTrainerId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.EventTrainerId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.EventTrainerId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
